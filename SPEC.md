# SPEC.md
diffwww (macOS / Tauri + React + TypeScript + Tailwind)

---

# 1. プロジェクト概要

## 1.1 目的

macOS向けに、シンプルで直感的な「差分比較ツール」を提供する。

比較対象は以下の3種類：

1. **ディレクトリ比較**：左右2つのフォルダを再帰的に比較
2. **gitコミット間比較**：同一リポジトリの2つのコミットを比較
3. **gitブランチ間比較**：同一リポジトリの2つのブランチを比較

本アプリは以下を重視する：

- 学習コストが低い
- 差分が一目で分かる
- 大規模フォルダ・大規模リポジトリでもフリーズしない

## 1.2 技術スタック

| レイヤー | 技術 |
|---------|------|
| デスクトップシェル | Tauri (Rust) |
| UI フレームワーク | React 18 + TypeScript |
| ビルドツール | Vite |
| スタイリング | Tailwind CSS |
| UIコンポーネント | shadcn/ui |
| アニメーション | framer-motion (motion/react) |
| アイコン | lucide-react |
| パッケージ管理 | pnpm |
| git 連携 | システム `git` CLI を Rust 側から実行 |

---

# 2. 設計原則

## 2.1 シンプル第一

- UIは明確な階層構造
- 機能はMVPから段階的に拡張
- 過剰設計をしない

## 2.2 責務分離

| 層 | 役割 |
|----|------|
| UI層（React） | 表示・操作 |
| IPC層（Tauri invoke） | フロントエンド ↔ バックエンド通信 |
| 比較エンジン（Rust/Tauri） | ファイルシステム走査・git操作・差分計算 |
| 状態管理（React useState） | モード・フィルタ・選択状態 |

## 2.3 モックファースト

- Figma Make で作成したデザインを UI の視覚的リファレンス（正）とする
- モックデータ（mockData.ts）でUIを先行開発する
- Tauri IPC は後続 issue で差し替える
- git比較モードのモックデータ（ブランチ一覧・コミット一覧・差分ツリー）も同様に先行整備する

## 2.4 比較結果の統一

ディレクトリ比較・git比較のどちらも、結果は同一のデータ型（`FileNode[]`）に正規化する。
これにより差分ツリー表示・詳細ビュー・フィルタ・検索・統計バッジを全モードで共通利用できる。

---

# 3. コアデータ型

```ts
export type CompareStatus = 'added' | 'deleted' | 'modified' | 'identical';

export type CompareMode = 'directory' | 'git';

export interface FileNode {
  name: string;
  path: string;
  type: 'file' | 'directory';
  status: CompareStatus;
  children?: FileNode[];
  size?: number;
  modifiedDate?: string;
  hash?: string;
  isText?: boolean;
  leftContent?: string;
  rightContent?: string;
}

export interface DiffLine {
  type: 'added' | 'deleted' | 'modified' | 'unchanged';
  leftLineNumber?: number;   // deleted / modified / unchanged で設定
  rightLineNumber?: number;  // added / modified / unchanged で設定
  leftContent?: string;      // added のとき undefined（プレースホルダ行）
  rightContent?: string;     // deleted のとき undefined（プレースホルダ行）
}

export interface GitBranch {
  name: string;        // 例: "main", "feature/xxx"
  isCurrent: boolean;
}

export interface GitCommit {
  hash: string;        // フルハッシュ
  shortHash: string;   // 先頭7桁
  message: string;     // 1行目のみ
  author: string;
  date: string;        // ISO 8601
}

// git比較の対象指定。ブランチ名またはコミットハッシュを指す
export interface GitRefSelection {
  branch: string;      // 選択中のブランチ名
  commit?: string;     // 未指定ならブランチの HEAD
}
```

---

# 4. MVP機能要件

## 4.1 比較モード選択

ヘッダーにタブを設け、2つの比較モードを切り替える：

| モード | 説明 |
|--------|------|
| フォルダ比較 | 左右のディレクトリを再帰的に比較（デフォルト） |
| git比較 | 1つのリポジトリ内で2つの ref（ブランチ/コミット）を比較 |

モード切り替え時は入力パス・選択 ref・比較結果・エラーをすべてリセットする。

## 4.2 フォルダ比較：パス入力

- 左右2ペインでフォルダパスを入力
- Tauri IPC 経由でOSネイティブのフォルダ選択ダイアログを呼び出す
- Finder からウィンドウにドラッグ＆ドロップしてパスを入力できる（ドロップ位置の左右で自動振り分け）

## 4.3 git比較：リポジトリ・ref 選択

- リポジトリパスを1つ入力（フォルダ選択ダイアログ or Finder D&D）
- 入力パスが git リポジトリでない場合はエラーメッセージを表示する
- リポジトリ確定後、左右それぞれで比較対象 ref を選択する：
  - **ブランチセレクト**：`list_branches` の結果から選択
  - **コミットセレクト**：選択ブランチの最新コミット一覧（最大50件、shortHash + メッセージ + 日付）から選択。未選択ならブランチ HEAD
- 左＝比較元（old）、右＝比較先（new）とする
  - 例：左 `main` / 右 `feature/x` → 「feature/x で追加したファイル」が `added` になる

## 4.4 比較ステータス分類

| ステータス | 意味 | 色 |
|-----------|------|----|
| `added` | 右（B/new）にのみ存在 | 緑 |
| `deleted` | 左（A/old）にのみ存在 | 赤 |
| `modified` | 両方存在するが内容が異なる | 黄 |
| `identical` | 完全一致 | グレー |

git比較では、差分のないファイルはツリーに含めない（`identical` は git 比較では原則出現しない。リポジトリ全ファイルの列挙は行わない）。

## 4.5 差分ツリー表示（DirectoryTree）

- ツリー形式でファイル・ディレクトリを表示
- ステータス別カラー表示（左ボーダー + 背景グラデーション）
- ディレクトリはクリックで開閉
- 選択ファイルはハイライト表示
- 全モード共通コンポーネント（入力は `FileNode[]` のみ）

## 4.6 詳細ビュー（FileDetailView）

### テキストファイル（TextDiffView）

- 左右2カラムのSplit diff表示（行の種類は WinMerge 流の4分類）
- 行番号表示
- 追加行（`added`）：右カラムのみ緑背景、左カラムは対応位置にプレースホルダ行
- 削除行（`deleted`）：左カラムのみ赤背景、右カラムは対応位置にプレースホルダ行
- 変更行（`modified`）：左右両方に存在し内容が異なる行。左右同一行位置に黄（amber）背景で表示
  - Myers 差分（`similar` クレート）の Replace 操作を `modified` にマップする
- 変更なし（`unchanged`）：透明
- プレースホルダ行：行番号なし・一段濃い背景＋斜線パターン。左右の行位置が常に揃うことを保証する

#### 長い行の表示（重要：Peekdiff での教訓）

Peekdiff では行がペイン幅を超えると右に切れて閲覧できなかった。diffwww では以下を必須とする：

- 長い行は折り返さない（word-wrap しない）
- 各ペインに水平スクロールを設け、**左右ペインの水平スクロールを同期**する（垂直スクロールも同期）
- 行番号カラムは水平スクロール時も左端に固定（sticky）する
- 水平スクロールバーは overflow 時に常時表示し、スクロール可能であることが分かるようにする
- 追加行・削除行の背景色は行の論理的な全幅（スクロール領域全体）に適用する（表示幅で途切れさせない）

### バイナリファイル（BinaryFileView）

- ファイルサイズ（KB）
- ハッシュ値（ディレクトリ比較：SHA-256 / git比較：blob ハッシュ）
- 最終更新日時（git比較ではコミット日時）

## 4.7 フィルタ機能

- すべて / 追加 / 削除 / 変更 / 同一 をセレクトで切り替え
- git比較モードでは「同一」選択肢を非表示にする

## 4.8 検索

- ファイル名部分一致でツリーをリアルタイムフィルタ

## 4.9 比較実行

- 「比較」ボタンで再スキャン
- スキャン中はボタン無効化 + プログレスバー表示
- 完了後に統計バッジを更新

## 4.10 統計バッジ

ヘッダーに常時表示：

- `+N`（追加数）
- `-N`（削除数）
- `~N`（変更数）
- `=N`（同一数、フォルダ比較のみ）

---

# 5. UI設計

## 5.1 レイアウト構成

### フォルダ比較モード

```
┌─────────────────────────────────────────────────────┐
│ Header                                               │
│  🔍 diffwww  [フォルダ比較 | git比較]                  │
│  ┌─────────────────┐ ┌─────────────────┐ ┌──────┐  │
│  │ 📁 左フォルダパス │ │ 📁 右フォルダパス │ │ 比較 │  │
│  └─────────────────┘ └─────────────────┘ └──────┘  │
│  [プログレスバー（比較中のみ）]                         │
│  ┌──────────────────────┐ ┌──────────────────────┐  │
│  │ 🔍 検索               │ │ 🔽 フィルタ            │  │
│  └──────────────────────┘ └──────────────────────┘  │
│  [+N] [-N] [~N] [=N]  ← 統計バッジ                  │
├────────────────────────┬────────────────────────────┤
│ DirectoryTree (w-96)   │ FileDetailView (flex-1)    │
│  ツリー形式             │  Split Diff / Binary Info  │
└────────────────────────┴────────────────────────────┘
```

### git比較モード

```
┌─────────────────────────────────────────────────────┐
│ Header                                               │
│  🔍 diffwww  [フォルダ比較 | git比較]                  │
│  ┌──────────────────────────────────────┐            │
│  │ 📁 リポジトリパス                       │            │
│  └──────────────────────────────────────┘            │
│  ┌────────────────────┐ ┌────────────────────┐ ┌──────┐
│  │ A: 🌿ブランチ ▾      │ │ B: 🌿ブランチ ▾      │ │ 比較 │
│  │    ⎇ コミット ▾     │ │    ⎇ コミット ▾     │ └──────┘
│  └────────────────────┘ └────────────────────┘        │
│  [プログレスバー（比較中のみ）]                         │
│  [検索] [フィルタ]  [+N] [-N] [~N]                    │
├────────────────────────┬────────────────────────────┤
│ DirectoryTree (w-96)   │ FileDetailView (flex-1)    │
│  変更ファイルツリー      │  Split Diff / Binary Info  │
└────────────────────────┴────────────────────────────┘
```

## 5.2 デザイン方針

Peekdiff（ライト基調・パープル〜ピンクのグラデーション）と明確に差別化する。
「閲覧専用の WinMerge」として、開発者向けのプロフェッショナルなツール然としたデザインとする。

- カラーテーマ：**ダークテーマ基調**（コードエディタライク。VS Code / GitHub Dark 系）
- 背景：濃いチャコール／スレート系のソリッドカラー（グラデーション不使用）
- アクセントカラーは1色のみ（ティール／シアン系）。選択・フォーカス・プライマリボタンに限定して使用
- フラットデザイン：装飾的なグラデーション・シャドウ・Confetti 等の演出は使わない
- コード・パス・差分・ハッシュは等幅フォント、UIラベルはサンセリフ
- 情報密度は高め、角丸は小さめ（4〜6px）
- ステータス色は全モード共通（追加=green, 削除=red, 変更=yellow, 同一=gray。ダーク背景向けに彩度・透過を調整）
- アニメーション：framer-motion で最小限（フェードインのみ）
- Figma Make で作成したデザインを視覚的リファレンス（正）とし、細部の色・余白はそれに従う
  - デザインファイル：https://www.figma.com/make/gRePDLuZExkMopPxTJQ5Gr/diffwww-UI-Design
  - Screen 01: フォルダ比較 / Screen 02: git比較 / Screen 03: 空状態 / Screen 04: バイナリ詳細

### 主要カラートークン（Figma Make 確定値）

| トークン | 値 | 用途 |
|---------|-----|------|
| `--background` | `#1a1d23` | ウィンドウ背景・diff エリア |
| `--card` / パネル | `#23272e` / `#20242b` | ヘッダー・ツリーペイン・カード |
| `--muted` / 入力欄 | `#171a20` | 入力欄・セレクト背景 |
| ガター | `#15181e` | 行番号ガター・スクロールバートラック |
| `--border` | `#333842` | 境界線全般 |
| `--primary` | `#2dd4bf` | 選択タブ・比較ボタン・選択行・フォーカスリング |
| `--foreground` | `#d7dde7` | 基本テキスト |
| 追加 | emerald 系（`/10`前後の低透過） | バッジ・ツリー・diff 行 |
| 削除 | rose 系（同上） | バッジ・ツリー・diff 行 |
| 変更 | amber 系（同上） | バッジ・ツリー・diff 行・ハッシュ差異ハイライト |
| 同一 | zinc 系 | バッジ・ツリー |

フォント：UI = Inter / コード・パス・ハッシュ・行番号 = JetBrains Mono

## 5.3 コンポーネント構成

```
App
├── Header
│   ├── ModeTabs（フォルダ比較 / git比較）
│   ├── [フォルダ比較] PathInput × 2
│   ├── [git比較] RepoInput × 1 + GitRefSelector × 2（ブランチ + コミット）
│   ├── CompareButton
│   ├── ProgressBar（比較中のみ）
│   ├── SearchInput
│   ├── FilterSelect
│   └── StatsBadges
├── DirectoryTree（全モード共通）
│   └── TreeNode（再帰）
└── FileDetailView（全モード共通）
    ├── TextDiffView
    └── BinaryFileView
```

---

# 6. 非機能要件

## 6.1 パフォーマンス

- 比較はTauriバックエンド（Rust）で実行し、UIをブロックしない
- ディレクトリ比較の段階比較アルゴリズム：
  1. パス存在比較
  2. サイズ比較
  3. ハッシュ比較（サイズが一致した場合）
  4. テキスト判定後diff生成
- git比較は `git diff --name-status A B` で変更ファイル一覧を取得し、ファイル内容は選択時に `git show REF:PATH` で遅延取得する（全ファイルの内容を先読みしない）
- テキストdiffはMyers差分アルゴリズム（`similar` クレート）をRustバックエンドで実行し、フロントエンドへ `DiffLine[]` として返す
- ハッシュ：SHA-256、大容量ファイルはストリーム処理
- テキストファイルの読み込み上限：1 MB（超過時はメッセージ表示）
- コミット一覧の取得は最大50件

## 6.2 Tauri IPC コマンド一覧

### 共通

| コマンド | 説明 |
|---------|------|
| `open_folder_dialog` | OSネイティブのフォルダ選択ダイアログを開く |
| `compute_diff` | 左右テキスト文字列をMyers差分計算し `DiffLine[]` を返す |

### フォルダ比較

| コマンド | 説明 |
|---------|------|
| `compare_directories` | 左右ディレクトリを再帰比較し `FileNode[]` を返す |
| `read_file_content` | 指定パスのテキストファイルを読み込む |
| `read_binary_meta` | 指定パスのバイナリメタデータ（サイズ / SHA-256 / 更新日時）を返す。BinaryFileView の左右カード表示用（FileNode はハッシュを片側分しか保持しないため） |

### git比較

| コマンド | 説明 |
|---------|------|
| `is_git_repository` | 指定パスが git リポジトリか判定する |
| `list_branches` | ブランチ一覧を `GitBranch[]` で返す |
| `list_commits` | 指定ブランチの最新コミットを `GitCommit[]`（最大50件）で返す |
| `compare_git_refs` | 2つの ref を比較し変更ファイルの `FileNode[]` を返す |
| `read_file_at_ref` | `git show REF:PATH` で指定 ref のファイル内容を読み込む |

## 6.3 git 連携方式

- git 操作はシステムにインストール済みの `git` CLI を Rust 側から `std::process::Command` で実行する
  - macOS では Xcode Command Line Tools により git が利用可能である前提とする
  - `git2`（libgit2）クレートは MVP では採用しない（依存とビルドの複雑さを避ける）
- 実行時は必ず `-C <repoPath>` でリポジトリを指定し、カレントディレクトリに依存しない
- 対象リポジトリへの**書き込み操作は一切行わない**（読み取り専用：`diff` / `show` / `branch` / `log` / `rev-parse` のみ使用可）
- git コマンドの失敗（不正な ref、壊れたリポジトリ等）は stderr を含むエラーメッセージをフロントエンドに返す

## 6.4 macOS最適化

- Retina対応
- Tauri標準ウィンドウ、最小サイズ 640×500
- Finder D&Dはウィンドウの左半分 / 右半分でパス振り分け（Tauri `onDragDropEvent` 使用）
  - git比較モードではリポジトリ入力欄が1つのため、ドロップ位置によらずリポジトリパスとして扱う

## 6.5 安定性

- エラー時はクラッシュしない
- アクセス不可ファイルはスキップしてログ表示
- ディレクトリ比較では `.git` / `node_modules` / `target` 等は自動スキップ
- git比較中にリポジトリ状態が変わっても（コミット追加等）、表示中の結果は保持する
